import { PageStub } from "../components/page-stub";

export const metadata = { title: "Enterprise - NovaOS" };

export default function EnterprisePage() {
  return (
    <PageStub
      eyebrow="Enterprise"
      title="Built for the most ambitious teams."
      intro="Security, scale, and control, presented the way a billion-dollar brand should. The full Enterprise story lands next."
    />
  );
}

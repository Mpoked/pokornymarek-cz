import { PageStub } from "../components/page-stub";

export const metadata = { title: "AI Features - NovaOS" };

export default function AIFeaturesPage() {
  return (
    <PageStub
      eyebrow="Intelligence"
      title="An assistant that understands your work."
      intro="The full story of how NovaOS thinks with you, from drafts in your voice to workflows that run themselves. Arriving next."
    />
  );
}

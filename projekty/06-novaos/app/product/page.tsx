import { PageStub } from "../components/page-stub";

export const metadata = { title: "Product - NovaOS" };

export default function ProductPage() {
  return (
    <PageStub
      eyebrow="Product"
      title="One system for everything you make."
      intro="A full tour of the NovaOS workspace, told benefits first. Coming to this page next, with the same craft as the homepage."
    />
  );
}

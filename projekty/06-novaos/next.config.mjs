/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // picsum.photos placeholders served unoptimized. Swap for real assets.
    unoptimized: true,
  },
};

export default nextConfig;

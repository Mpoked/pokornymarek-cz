services:
  web:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      - DB_PATH=/app/data/studio.db
    volumes:
      - ./data:/app/data

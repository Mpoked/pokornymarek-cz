import { PageStub } from "../components/page-stub";

export const metadata = { title: "About - NovaOS" };

export default function AboutPage() {
  return (
    <PageStub
      eyebrow="About"
      title="We build tools that feel inevitable."
      intro="The people, the principles, and the belief that work should feel calm. The full About story is being written next."
    />
  );
}
